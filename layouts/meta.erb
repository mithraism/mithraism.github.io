<% timestamp = Time.parse((@article[:created_at] || @article[:created_on]).to_s) %>
<p class="meta">
	<%= @verb || 'Written' %> on
	<%= timestamp.mon.to_mon_s %>
	<%= timestamp.mday.ordinal %>,
	<%= timestamp.year %>.
</p>
